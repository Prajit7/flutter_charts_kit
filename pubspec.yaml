name: flutter_charts_kit
description: A generic, interactive 3D surface chart widget for Flutter — rotate, zoom, hover/tap tooltips, calibrated color gradients.
version: 0.0.1
homepage: https://github.com/prajit7/flutter_charts_kit
repository: https://github.com/prajit7/flutter_charts_kit
issue_tracker: https://github.com/prajit7/flutter_charts_kit/issues
topics:
  - chart
  - 3d
  - visualization
  - widgets

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
