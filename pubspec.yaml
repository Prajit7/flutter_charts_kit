name: surface_3d_chart
description: A generic, interactive 3D surface chart widget for Flutter — rotate, zoom, hover/tap tooltips, calibrated color gradients.
version: 0.6.1
homepage: https://github.com/prajit7/surface_3d_chart
repository: https://github.com/prajit7/surface_3d_chart
issue_tracker: https://github.com/prajit7/surface_3d_chart/issues
topics:
  - chart
  - 3d
  - visualization
  - widgets

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
